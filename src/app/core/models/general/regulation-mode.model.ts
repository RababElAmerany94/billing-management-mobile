/**
 * a interface describe regulation mode
 */
export interface IRegulationMode {
    id: string;
    name: string;
    isModify: boolean;
    classement: number;
}

/**
 * a interface describe regulation mode model
 */
export interface IRegulationModeModel {
    name: string;
    isModify: boolean;
    classement: number;
}
